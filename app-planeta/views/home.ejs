<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        width: 100vw;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
      }
      th, td {
        padding: 5px;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <table>
      <tr>
        <td>Corpo</td>
        <td>Diâmetro</td>
        <td>Distância média do Sol (km)</td>
      </tr>
      <% for (let i = 0; i < planetas.length; i++) { %>
      <tr>
        <td><%= planetas[i].nome %></td>
        <td><%= planetas[i].diametro %></td>
        <td><%= planetas[i].distanciaSol %></td>
      </tr>
      <% } %>
    </table>
  </body>
</html>
